import { Prisma, UiScope, ValueType } from "@prisma/client";

// Explicitly typed so the array stays well-typed even when a module strip empties it.
export const seed_configs: Prisma.ConfigCreateInput[] = [
]
